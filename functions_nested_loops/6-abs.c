#include "main.h"

/**
 * _abs - Entry point
 * @n: int code will check for this character
 *
 * Return: the absolute value of the intinger
 */

int _abs(int n)
{
	if (n <= -1)
	{
		n *= -1;
	}
	return (n);
}
